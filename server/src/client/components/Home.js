import React from 'react'

const Home = () => {
    return (
        <div>
            <div>Hello from nodemon</div>
            <button onClick={() => console.log('test')}>Click</button>
        </div>
    )
}

export default Home